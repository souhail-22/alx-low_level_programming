#include "main.h"

/**
 * _abs - Computes the absolut value of an integer
 * @t: The integer to be computed
 * Return: The absolute value
 */
int _abs(int t)

{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
